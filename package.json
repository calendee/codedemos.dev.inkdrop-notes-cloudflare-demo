{
	"private": true,
	"name": "inkdrop-notes",
	"version": "1.0.0",
	"description": "A sample for serving HTML from the Inkdrop API endpoint.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "prettier --write '**/*.{js,css,json,md}'"
	},
	"author": "Justin Noel <justin.noel@calendee.com>",
	"license": "MIT",
	"devDependencies": {
		"prettier": "^1.18.2"
	},
	"dependencies": {
		"itty-router": "^2.1.9"
	}
}
